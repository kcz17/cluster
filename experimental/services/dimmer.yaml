apiVersion: v1
kind: Service
metadata:
  name: dimmer
  labels:
    name: dimmer
  namespace: sock-shop
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 8078
      nodePort: 30002
  selector:
    name: dimmer
